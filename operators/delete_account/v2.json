{
  "artifact_id": "delete_account",
  "version": 2,
  "title": "Delete account",
  "description": "Sign in and delete a Checking or Savings product from the dashboard. Nonzero balances must be transferred first by the calling workflow.",
  "locator_policy": "dom_only",
  "viewport": {
    "profile": "desktop",
    "width": 1280,
    "height": 900
  },
  "viewports_validated": [
    "desktop"
  ],
  "error_handling": {
    "page_not_found": {
      "description": "Target route returned 404 / page not found.",
      "policy": "one_retry",
      "delays_sec": [
        1
      ],
      "max_attempts": 1,
      "recorded_occurrences": 1,
      "last_message": "Handled page_not_found for member casey404",
      "last_status": "recovered"
    }
  },
  "inputs": {
    "member_id": {
      "type": "string",
      "required": true,
      "description": "Opaque member login id: letters followed by exactly three digits. Do not store real personal names in artifacts; supply this at invoke time.",
      "pattern": "^[A-Za-z]+\\d{3}$",
      "example": "alex123"
    },
    "account": {
      "type": "string",
      "required": true,
      "description": "Product to delete.",
      "enum": [
        "Checking",
        "Savings"
      ],
      "example": "Savings"
    },
    "bank_url": {
      "type": "string",
      "required": false,
      "description": "Atlas Bank login URL for this environment.",
      "example": "http://localhost:5173/login"
    }
  },
  "outputs": {
    "account_status": {
      "type": "string",
      "description": "Delete result message.",
      "example": "Savings account deleted."
    }
  },
  "steps": [
    {
      "id": "step_1",
      "type": "click",
      "description": "Return to dashboard \u2014 click Dashboard",
      "risk": "low",
      "target": {
        "strategy": "role",
        "role": "button",
        "name": "Dashboard",
        "robustness": "Locate by ARIA role + accessible name so the control remains findable if layout shifts but semantics stay the same.",
        "interaction": "dom"
      }
    },
    {
      "id": "step_2",
      "type": "click",
      "description": "Delete account \u2014 click Delete {{account}} Account",
      "risk": "high",
      "target": {
        "strategy": "role",
        "role": "button",
        "name": "Delete {{account}} Account",
        "robustness": "Locate by ARIA role + accessible name so the control remains findable if layout shifts but semantics stay the same.",
        "interaction": "dom"
      }
    },
    {
      "id": "step_3",
      "type": "read",
      "description": "Read status message \u2014 read open-account-message",
      "risk": "low",
      "target": {
        "strategy": "testid",
        "value": "open-account-message",
        "robustness": "Prefer data-testid attributes; they are stable across copy changes and less brittle than visible text.",
        "interaction": "dom"
      }
    }
  ],
  "success_conditions": [
    {
      "id": "member_details_visible",
      "description": "Signed-in member chrome is visible on the page.",
      "required": true
    },
    {
      "id": "checking_balance_visible",
      "description": "Checking account balance card/text is present.",
      "required": true
    },
    {
      "id": "savings_balance_visible",
      "description": "Savings account balance card/text is present.",
      "required": true
    },
    {
      "id": "account_deleted",
      "description": "The account is gone or a delete confirmation message is shown.",
      "required": true
    },
    {
      "id": "account_opened",
      "description": "The requested product account card is visible after open.",
      "required": true
    }
  ],
  "query_signatures": [
    "delete the savings account"
  ]
}
